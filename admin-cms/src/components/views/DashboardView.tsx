import styled from "styled-components";

const DashboardView = () => {
  return <Container>DashboardView</Container>;
};

export default DashboardView;

const Container = styled.main`
  padding: 1rem;
  background-color: transparent;
`;
